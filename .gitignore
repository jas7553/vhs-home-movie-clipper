# Python
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.coverage
coverage.json

# Video source files and output — not part of the tool
*.mp4
*_clips/
*_ocr_cache.json
*_visual_cache.json
*_scene_score_cache.json
!tests/fixtures/*_ocr_cache.json

# Compiled binary — build from ocr_timestamp.swift
ocr_timestamp

# macOS
.DS_Store

# Claude Code project settings
.claude/

# Secrets — never commit
.env
.env.*
*.pem
*_credentials.json

# Local scratch: caches, generated clips, preserved readings backups (multi-GB)
archive/

# Local scratch: issue notes, probe/measurement scripts, montages — not part of the tool
.scratch/
