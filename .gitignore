# Python
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.coverage

# Video source files and output — not part of the tool
*.mp4
*_clips/
*_ocr_cache.json
!tests/fixtures/*_ocr_cache.json

# Compiled binary — build from ocr_timestamp.swift
ocr_timestamp

# macOS
.DS_Store

# Claude Code project settings
.claude/

# Secrets — never commit
.env
.env.*
*.pem
*_credentials.json

# Vision-refine prototype scratch (exported frames + manifest)
vision_frames/

# Local scratch: caches, generated clips, preserved readings backups (multi-GB)
archive/
